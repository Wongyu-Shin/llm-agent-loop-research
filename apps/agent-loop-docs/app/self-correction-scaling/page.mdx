import {
  PartBoundary,
  PersistentConclusion,
  PresenterNotes,
  ProvenanceBadge,
  ResearchDeck,
  ResearchHero,
  StoryScene,
} from "@/components/self-correction/presentation-shell"
import { IterationDrum } from "@/components/self-correction/scene-01-iteration-drum"
import {
  PaperScopeBand,
  TwoStateTransitionLab,
} from "@/components/self-correction/scene-02-transition-lab"
import { ConvergenceMapLab } from "@/components/self-correction/scene-03-convergence-map"
import { PredictionReplayLab } from "@/components/self-correction/scene-04-prediction-replay"
import { TheoryToLoopBridge } from "@/components/self-correction/scene-05-theory-bridge"
import {
  AutoresearchLoopMap,
  OfficialExampleStrip,
} from "@/components/self-correction/scene-06-loop-map"
import { LoopControlWorkbench } from "@/components/self-correction/scene-07-control-workbench"
import { LoopPolicyCard } from "@/components/self-correction/scene-08-policy-card"

export const metadata = {
  title: "LLM 자기수정을 위한 확률적 추론 스케일링 이론",
  description:
    "A Probabilistic Inference Scaling Theory for LLM Self-Correction를 먼저 이해하고, 복구 이득과 훼손 손실의 멘탈 모델을 Autoresearch형 agent loop의 검증·채택·중단 정책으로 옮기는 25분 인터랙티브 문서입니다.",
}

<ResearchDeck>
  <ResearchHero />

  <StoryScene
    id="scene-01"
    part="paper"
    eyebrow="질문"
    title="논문은 “다시 생각하면 언제 좋아지는가”를 묻는다"
    visual={<IterationDrum />}
  >
    <ProvenanceBadge kind="paper-definition">논문 질문</ProvenanceBadge>

    같은 모델이 같은 답을 반복해서 수정할 때, 정확도는 왜 상승하기도 하고
    정체하거나 하락하기도 할까요? 여기서 scaling은 모델을 더 크게 학습하는
    일이 아니라 추론 시점의 수정 라운드를 늘리는 일입니다. 논문은 오답이
    정답으로 들어오는 **복구 유입**과 정답이 오답으로 빠져나가는 **훼손
    유출**을 따로 측정한 뒤, 두 흐름을 하나의 전이식으로 묶어 여러 라운드의
    정확도 궤적을 설명합니다.

    <PersistentConclusion badge="paper-definition">
      반복 횟수만으로는 개선 방향이 결정되지 않는다.
    </PersistentConclusion>

    <PresenterNotes sceneId="scene-01" />
  </StoryScene>

  <StoryScene
    id="scene-02"
    part="paper"
    eyebrow="상태 전이"
    title="두 조건부 확률로 다음 정확도를 회계한다"
    visual={<TwoStateTransitionLab />}
  >
    <ProvenanceBadge kind="paper-definition">Eq. 6 재인덱싱</ProvenanceBadge>

    논문이 직접 다루는 대상은 외부 정보 없이 같은 모델이 이전 응답을 다시
    검토하는 intrinsic self-correction입니다.

    <PaperScopeBand />

    라운드 $t$의 정답 비율은 $Acc_t$입니다. 현재 정답이 수정 뒤에도 정답으로
    남을 조건부 확률이 $CL_t$(정답 보존율), 현재 오답이 정답으로 바뀔 조건부
    확률이 $CS_t$(오답 복구율)입니다. 원래 명칭은 Confidence Level과 Critique
    Score이지만, 둘 다 모델이 말로 보고한 자신감이나 critique 품질 점수가
    아닙니다. 외부 정답 판정이 끝난 뒤 응답 집단에서 계산한 조건부
    전이율입니다.

    <div
      className="scene-formula"
      role="math"
      aria-label="논문 식 6을 t에서 t 더하기 1 방향으로 재인덱싱한 식. 다음 정확도는 현재 정확도 곱하기 CL 아래첨자 t 더하기 현재 오답률 곱하기 CS 아래첨자 t."
    >
      <span className="scene-formula-label">논문 Eq. 6 · $t-1 \to t$를 화면 진행 방향 $t \to t+1$로 재인덱싱</span>
      <span className="scene-equation">$\displaystyle Acc_{t+1} = Acc_t \times CL_t + (1 - Acc_t) \times CS_t$</span>
    </div>

    <PersistentConclusion badge="paper-definition">
      다음 정확도는 살아남은 정답과 새로 복구된 정답의 합이다.
    </PersistentConclusion>

    <PresenterNotes sceneId="scene-02" />
  </StoryScene>

  <StoryScene
    id="scene-03"
    part="paper"
    eyebrow="수렴 지도"
    title="기대를 충족하는 조건 공간은 좁다"
    visual={<ConvergenceMapLab />}
  >
    <ProvenanceBadge kind="paper-mathematical-consequence">정지 가정과 고정점</ProvenanceBadge>

    한 라운드의 순변화는 회계식에서 그대로 따라오므로, 논문의 실제 기여는
    반복의 목적지를 닫힌식으로 답한 데 있습니다. $CL_t$와 $CS_t$가 라운드와
    무관한 상수 $CL$, $CS$라고 가정하면 궤적은 고정점 $Upp$로 수렴하고,
    따름정리에 따라 이 목적지는 초기 정확도와 무관하게 $CL$과 $CS$만이
    정합니다.

    <div
      className="scene-formula scene-formula-grid"
      role="math"
      aria-label="정지 전이 모형의 세 식. Upp, 알파, 닫힌형 정확도 궤적."
    >
      <span className="scene-equation">$\displaystyle Upp = \frac{CS}{1 - CL + CS}$</span>
      <span className="scene-equation">$\displaystyle \alpha = CL - CS$</span>
      <span className="scene-equation">$\displaystyle Acc_t = Upp - \alpha^t \times (Upp - Acc_0)$</span>
    </div>

    같은 목적지의 $(CL, CS)$ 조합은 $CL=1$, $CS=0$ 모서리를 지나는 직선이라,
    높은 기대를 충족하는 조합은 모서리 옆 좁은 쐐기에 몰립니다 — 목적지 90%
    이상은 전체 조건 공간의 약 5.6%, 시작 99%에서 반복이 이득인 조합은 약
    0.5%뿐입니다. 흔히 말하는 손익분기 $CL$ 약 99.49%는 별도 공식이 아니라
    경계선 $Upp = Acc_0$ 위의 한 점입니다. $CL=1$, $CS=0$이면 분모가 0이어서
    단일 고정점 공식을 적용하지 않고, 빠르게 수렴하는 것과 높은 정확도로
    수렴하는 것은 같은 말이 아닙니다.

    <PersistentConclusion badge="paper-mathematical-consequence">
      목적지는 시작점이 아니라 절차가 정하며, 기대를 충족하는 조건 공간은
      생각보다 좁다.
    </PersistentConclusion>

    <PresenterNotes sceneId="scene-03" />
  </StoryScene>

  <StoryScene
    id="scene-04"
    part="paper"
    eyebrow="검증과 주장 경계"
    title="곡선 일치는 어디까지가 예측인가"
    visual={<PredictionReplayLab />}
  >
    <ProvenanceBadge kind="paper-observation">실험 절차 · 실측 곡선</ProvenanceBadge>
    <ProvenanceBadge kind="critical-reading">주장 강도</ProvenanceBadge>

    논문은 질문마다 5개 응답을 샘플링해 5라운드 자기수정을 실행하고, 첫
    전이에서 추정한 $Acc_0$, $CL$, $CS$로 이후 궤적을 계산해 실제 곡선과
    비교했습니다. 첫 수정 뒤의 $Acc_1$은 같은 전이 데이터가 거의 그대로
    결정하므로 보정에 가깝고, 예측력은 2~5라운드의 일치에서 판단해야
    합니다. 8개 모델과 8개 데이터셋을 사용했지만 가능한 64개 조합 전체를
    실행하지는 않았고, RMSE·R²·신뢰구간 같은 정량 적합도 지표는 보고하지
    않았습니다.

    시각화의 실측 곡선은 논문 그림에서 눈으로 읽은 근사값이며, 이론 곡선은
    역산한 $CL$·$CS$로 닫힌식을 재계산한 것입니다.

    <PersistentConclusion badge="critical-reading">
      논문은 제한된 intrinsic self-correction에서 유용한 최소 모형을 보였지만,
      모든 최신 Agent가 같은 고정 곡선을 따른다고 입증하지 않았다.
    </PersistentConclusion>

    <PresenterNotes sceneId="scene-04" />
  </StoryScene>

  <StoryScene
    id="scene-05"
    part="bridge"
    eyebrow="적용 경계"
    title="고정 곡선이 아니라 복구와 훼손을 묻는 습관을 옮긴다"
    visual={<TheoryToLoopBridge />}
    boundary={<PartBoundary />}
  >
    <ProvenanceBadge kind="critical-reading">여기부터 엔지니어링 적용</ProvenanceBadge>

    논문이 분석하는 단위는 같은 응답을 다시 고치는 한 라운드입니다. 반면 실무
    loop가 다루는 단위는 격리된 artifact experiment입니다. 그러므로
    정답·오답을 challenger와 incumbent에 바로 대응시켜서는 안 됩니다. 사례별
    pass/fail 기준이 있을 때만 $CL_t$와 $CS_t$를 계산할 수 있습니다. 연속형
    metric 하나뿐이라면 metric delta·불확실성·guard·holdout을 직접 비교해야
    합니다.

    <PersistentConclusion badge="critical-reading">
      candidate가 만든 변화와 evidence gate를 통과한 뒤의 system state 변화를
      분리한다.
    </PersistentConclusion>

    <PresenterNotes sceneId="scene-05" />
  </StoryScene>

  <StoryScene
    id="scene-06"
    part="practice"
    eyebrow="구조"
    title="무엇을 고정하고, 무엇을 바꾸며, 누가 채택하는가"
    visual={<AutoresearchLoopMap />}
  >
    <ProvenanceBadge kind="official-autoresearch-example">공식 사례</ProvenanceBadge>
    <ProvenanceBadge kind="engineering-transfer">일반화</ProvenanceBadge>

    아래 표는 공식 README와 `program.md`가 직접 명시한 기본 사례입니다.

    <OfficialExampleStrip />

    이 구조를 실무로 넓히려면 research contract, incumbent, isolated
    challenger, raw observation, verdict, append-only experiment ledger,
    derived memory를 별도 객체로 분리해 둬야 합니다. 공식 에이전트 지침
    문서(`program.md`)는 proposal policy이고 `results.tsv`는 raw ledger이므로
    둘을 압축된 derived memory와 동일시하지 않습니다. 그리고 KEEP 판정을 받은
    challenger만 다음 incumbent가 되게 합니다.

    <PersistentConclusion badge="engineering-transfer">
      Autoresearch형 loop의 단위는 생각 한 번이 아니라 고정된 계약 아래
      실행하고 되돌릴 수 있는 실험 한 건이다.
    </PersistentConclusion>

    <PresenterNotes sceneId="scene-06" />
  </StoryScene>

  <StoryScene
    id="scene-07"
    part="practice"
    eyebrow="통제"
    title="KEEP·DISCARD·CRASH 뒤에 무엇이 실제로 바뀌는가"
    visual={<LoopControlWorkbench />}
  >
    <ProvenanceBadge kind="engineering-transfer">replay · diagnose · stop</ProvenanceBadge>

    KEEP이면 challenger와 incumbent가 함께 전진합니다. DISCARD나 CRASH이면
    incumbent는 그대로 두고 실패 증거만 ledger에 추가합니다. 이 구분을 먼저
    지켜야 합니다. 그런 다음 proposer·executor·evaluator·controller의 경계를
    나눠서 진단합니다. 한 trial의 timeout과 전체 campaign의
    성공·안전·cycle·plateau·예산·human gate 종료도 서로 다른 시계로 관리해야
    합니다. 그래야 마지막 candidate가 아니라 검증된 best-so-far를 반환할 수
    있습니다.

    <PersistentConclusion badge="engineering-transfer">
      candidate의 실패는 ledger를 바꾸지만, gate를 통과하기 전에는 incumbent를
      후퇴시키지 않는다.
    </PersistentConclusion>

    <PresenterNotes sceneId="scene-07" />
  </StoryScene>

  <StoryScene
    id="scene-08"
    part="practice"
    eyebrow="정책"
    title="목표·변경·증거·채택·기억·중단을 한 장에 쓴다"
    visual={<LoopPolicyCard />}
  >
    <ProvenanceBadge kind="synthetic-example">synthetic policy</ProvenanceBadge>

    loop policy를 쓸 때는 실제 goal과 proxy metric, mutable artifact와 frozen
    harness, trial budget과 campaign budget, raw observation과 score,
    experiment ledger와 derived memory를 각각 분리합니다. 여기에
    KEEP·DISCARD·CRASH·rollback, 구조화된 stop reason, 고위험 external
    action의 human gate를 명시합니다.

    <ol className="scene-final-takeaways">
      <li>논문은 반복 횟수보다 정답 보존과 오답 복구의 균형을 보게 한다.</li>
      <li>적용할 때는 candidate의 전이와 system-after-gate의 전이를 분리한다.</li>
      <li>좋은 agent loop는 많이 고치는 시스템이 아니라 증거 없는 변경을 채택하지 않는 시스템이다.</li>
    </ol>

    <PresenterNotes sceneId="scene-08" />
  </StoryScene>

  <footer className="deck-sources" aria-labelledby="deck-sources-title">
    <div>
      <span>PRIMARY SOURCES</span>
      <h2 id="deck-sources-title">근거와 적용 경계</h2>
    </div>
    <ul className="scene-source-list">
      <li>
        <a href="https://aclanthology.org/2025.emnlp-main.685/" target="_blank" rel="noreferrer">
          Yang et al. (2025), A Probabilistic Inference Scaling Theory for LLM Self-Correction
        </a>
        <span>논문의 정의, 수렴식, 단일 전이 추정, 실험 비교</span>
      </li>
      <li>
        <a href="https://github.com/karpathy/autoresearch" target="_blank" rel="noreferrer">
          karpathy/autoresearch README
        </a>
        <span>고정 파일, 변경 파일, 5분 실행 예산, val_bpb</span>
      </li>
      <li>
        <a href="https://github.com/karpathy/autoresearch/blob/master/program.md" target="_blank" rel="noreferrer">
          karpathy/autoresearch program.md
        </a>
        <span>baseline, results.tsv, KEEP·DISCARD·CRASH, revert와 기본 중단 정책</span>
      </li>
    </ul>
  </footer>
</ResearchDeck>
